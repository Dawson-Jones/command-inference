[package]
name = "cmdi"
version = "0.1.0"
edition = "2024"
description = "AI-powered shell command fixer using DeepSeek"
license = "MIT"
repository = "https://github.com/patchescamerababy/cmdi"
readme = "README.md"
keywords = ["cli", "ai", "shell", "command", "deepseek"]
categories = ["command-line-utilities"]

[dependencies]
reqwest = { version = "0.12", features = ["json", "blocking"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
